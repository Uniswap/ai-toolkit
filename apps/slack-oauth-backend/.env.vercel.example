# Vercel Environment Variables
# Copy this file to .env.local for local development with Vercel CLI
# Add these variables in Vercel Dashboard for production deployment

# Required Slack Configuration
SLACK_CLIENT_ID=your_slack_client_id_here
SLACK_CLIENT_SECRET=your_slack_client_secret_here
# No SLACK_BOT_TOKEN: with token rotation, bot tokens expire (~12h); the server
# uses the freshly-issued bot token from each OAuth exchange instead.

# OAuth Redirect URI (will be auto-set after deployment)
# Format: https://your-project-name.vercel.app/slack/oauth/callback
SLACK_REDIRECT_URI=https://your-project-name.vercel.app/slack/oauth/callback

# Security (generate with: openssl rand -hex 32)
SESSION_SECRET=your-32-character-session-secret-here

# Optional Configuration
NODE_ENV=production
LOG_LEVEL=info
NOTION_DOC_URL=https://your-documentation-url.com/setup

# Note: PORT is automatically handled by Vercel
# Note: CORS_ORIGIN can be set if you need cross-origin support