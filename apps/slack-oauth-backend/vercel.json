{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pwd && ls -a && cd ../../ && npm -g install npm@11.7.0 && npm install --include=dev",
  "buildCommand": "cd ../../ && npx nx build slack-oauth-backend --prod",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
