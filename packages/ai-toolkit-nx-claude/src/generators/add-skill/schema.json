{
  "$schema": "https://json-schema.org/schema",
  "$id": "AddSkill",
  "title": "Add Skill Generator",
  "description": "Creates a new Claude Code skill in the skills package",
  "type": "object",
  "properties": {
    "package": {
      "type": "string",
      "description": "Target package for the skill",
      "prompt-message": "📦 Which package should this skill be added to?"
    },
    "createNewPackage": {
      "type": "boolean",
      "description": "Create a new package for this skill",
      "default": false,
      "prompt-message": "➕ Would you like to create a new package instead?",
      "prompt-type": "confirm",
      "prompt-when": "package === '__create_new__'"
    },
    "newPackageName": {
      "type": "string",
      "description": "Name of the new package (e.g., backend, frontend, typescript)",
      "prompt-message": "📝 Enter the name for the new package (e.g., backend, frontend, typescript):",
      "prompt-when": "createNewPackage === true",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "newPackageDescription": {
      "type": "string",
      "description": "Description of the new package",
      "prompt-message": "📄 Enter a description for the new package:",
      "prompt-when": "createNewPackage === true"
    },
    "name": {
      "type": "string",
      "description": "Name of the skill (lowercase, hyphens allowed)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "prompt-message": "⚡ What should the skill be called? (kebab-case, e.g., code-review):",
      "pattern": "^[a-z][a-z0-9-]*$",
      "maxLength": 64
    },
    "description": {
      "type": "string",
      "description": "Description of what the skill does and when Claude should use it (max 1024 chars)",
      "prompt-message": "💬 Describe what this skill does and when Claude should use it:",
      "maxLength": 1024
    },
    "allowedTools": {
      "type": "string",
      "description": "Comma-separated list of tools this skill can use (optional)",
      "prompt-message": "🔧 What tools should this skill have access to? (e.g., Read, Grep, Glob - leave empty for all):"
    }
  },
  "required": []
}
