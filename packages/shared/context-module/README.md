# context-module

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build context-module` to build the library.

## Running unit tests

Run `nx test context-module` to execute the unit tests via [Vitest](https://vitest.dev/).
